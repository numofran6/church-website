import { Layout } from '../components/Layout';
import { Timeline } from '../components/Timeline';

const Events = () => {
	return (
		<Layout title={'Events'}>
			<Timeline />
		</Layout>
	);
};

export default Events;
